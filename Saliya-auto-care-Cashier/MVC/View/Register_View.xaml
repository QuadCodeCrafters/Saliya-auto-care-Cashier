<UserControl x:Class="Saliya_auto_care_Cashier.MVVM.View.Register_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Saliya_auto_care_Cashier.MVVM.View" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="0" To="-5" Duration="0:0:0.1" AutoReverse="True" RepeatBehavior="3x"/>
        </Storyboard>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/ScrollViewerStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <materialDesign:Card UniformCornerRadius="15" 
                             Background="{DynamicResource MaterialDesignPaper}"
                             Margin="-1" materialDesign:ShadowAssist.ShadowDepth="Depth3">
            <Grid>
                <TextBlock Text="Register" FontSize="30" FontWeight="SemiBold" Width="auto" Height="auto" Margin="28" HorizontalAlignment="Left" VerticalAlignment="Top"/>

                <StackPanel HorizontalAlignment="Left" Margin="60,100" Width="auto" Height="auto">

                    <!-- Vehicle Number -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Vehicle ID:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox x:Name="txtvehiclenum" 
                                 Width="300" 
                                 Height="55" 
                                 FontSize="16" 
                                 Margin="70,3,0,0"
                                 materialDesign:HintAssist.Hint="Enter The Vehicle Number" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}">
                            <TextBox.Triggers>
                                <EventTrigger RoutedEvent="TextBox.GotFocus">
                                    <BeginStoryboard Storyboard="{StaticResource ShakeAnimation}"/>
                                </EventTrigger>
                            </TextBox.Triggers>
                        </TextBox>
                    </StackPanel>

                    <!-- Vehicle Type -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Vehicle Type:" 
                                   FontSize="17" 
                                   FontWeight="SemiBold" 
                                   VerticalAlignment="Center" 
                                   Margin="0,0,10,0"/>
                        <ComboBox Width="300" 
                                  x:Name="txtvehicletype" 
                                  Height="57" 
                                  Margin="50,0,0,0"
                                  FontSize="16" 
                                  materialDesign:HintAssist.Hint="Select Vehicle Type" 
                                  BorderThickness="2" 
                                  BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}" 
                            SelectedItem="{Binding SelectedVehicleType}">
                            <ComboBox.ItemsSource>
                                <x:Array Type="{x:Type sys:String}">
                                    <sys:String>Car</sys:String>
                                    <sys:String>Truck</sys:String>
                                    <sys:String>Motorcycle</sys:String>
                                    <sys:String>Van</sys:String>
                                    <sys:String>SUV</sys:String>
                                </x:Array>
                            </ComboBox.ItemsSource>
                        </ComboBox>

                    </StackPanel>

                    <!-- Vehicle Model -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Vehicle Model:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox Width="300" 
                                  x:Name="txtvehiclemodel" 
                                  Height="55" 
                                  Margin="38,0,0,0"
                                  FontSize="16" 
                                  materialDesign:HintAssist.Hint="Select Vehicle Model" 
                                  BorderThickness="2" 
                                  BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}" 
                                  SelectedItem="{Binding SelectedVehicleModel}">
                            <ComboBox.ItemsSource>
                                <x:Array Type="{x:Type sys:String}">
                                    <sys:String>Sedan</sys:String>
                                    <sys:String>Coupe</sys:String>
                                    <sys:String>Hatchback</sys:String>
                                    <sys:String>SUV</sys:String>
                                    <sys:String>Convertible</sys:String>
                                    <sys:String>Pickup Truck</sys:String>
                                    <sys:String>Minivan</sys:String>
                                    <sys:String>Luxury Car</sys:String>
                                </x:Array>
                            </ComboBox.ItemsSource>
                        </ComboBox>
                    </StackPanel>

                    <!-- Customer Name -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Customer Name:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="300" 
                                 x:Name="txtcusname" 
                                 Height="55" 
                                 Margin="22,0,0,0"
                                 FontSize="16" 
                                 materialDesign:HintAssist.Hint="Enter Customer Name" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>

                    <!-- Customer Address -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Customer Address:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="300" 
                                 x:Name="txtcusaddress" 
                                 Height="55" 
                                 FontSize="16" 
                                 Margin="7,0,0,0"
                                 materialDesign:HintAssist.Hint="Enter Customer Address" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>

                    <!-- Customer NIC -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Customer N.I.C:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="300" 
                                 Margin="32,0,0,0"
                                 x:Name="txtcusNIC" 
                                 Height="55" 
                                 FontSize="16" 
                                 materialDesign:HintAssist.Hint="Enter Customer NIC" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>

                </StackPanel>

                <StackPanel HorizontalAlignment="Right" Margin="150,100.7" Width="auto" Height="auto">

                    <!-- Customer E-Mail -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Customer E-Mail:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="300" 
                                 Height="55" 
                                 x:Name="txtcusmail" 
                                 Margin="68,0,0,0"
                                 FontSize="16" 
                                 materialDesign:HintAssist.Hint="Enter Customer E-Mail" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>

                    <!-- Customer Phone Number -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Customer Phone Number:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="300" 
                                 Height="55" 
                                 x:Name="txtcusnumber" 
                                 FontSize="16" 
                                 materialDesign:HintAssist.Hint="Enter Customer Phone Number" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Emergency Contact:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="300" 
                                 Margin="48,0,0,0"
                                 x:Name="txtemergencycontact" 
                                 Height="57" 
                                 FontSize="16" 
                                 materialDesign:HintAssist.Hint="Enter Emergency Contact" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>

                    <!-- Special -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="Special:" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="300" 
                                 Margin="145,0,0,0"
                                 x:Name="txtcusspec" 
                                 Height="175" 
                                 FontSize="16" 
                                 materialDesign:HintAssist.Hint="Enter Special Notes" 
                                 BorderThickness="2" 
                                 BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                </StackPanel>

                <Button Content="Register" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="100" Height="40" Click="btn_registor" Margin="750,0,0,60"/>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
